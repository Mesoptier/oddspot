import React from 'react';
import ReactDOM from 'react-dom';
import AdminApp from './containers/AdminApp.jsx';

const root = document.getElementById('root');

ReactDOM.render((
  <AdminApp />
), root);
